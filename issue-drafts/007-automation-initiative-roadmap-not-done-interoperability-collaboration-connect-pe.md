# Automation Initiative Roadmap — Not Done › Interoperability & Collaboration (Connect People & Tools): Surface macro selections inside the ghost agent client so operators can trigger macros explicitly (hook into `code/components/AgentPanel/`).

- **Source:** [Automation Initiative Roadmap](docs/automation-initiative-roadmap.md)
- **Section:** Not Done › Interoperability & Collaboration (Connect People & Tools)

## Task
Surface macro selections inside the ghost agent client so operators can trigger macros explicitly (hook into `code/components/AgentPanel/`).

## Acceptance Criteria
- [ ] Surface macro selections inside the ghost agent client so operators can trigger macros explicitly (hook into `code/components/AgentPanel/`).
