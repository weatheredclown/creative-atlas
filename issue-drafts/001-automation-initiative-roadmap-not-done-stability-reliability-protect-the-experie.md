# Automation Initiative Roadmap — Not Done › Stability & Reliability (Protect the Experience): Thread CRDT-aware operations through the adapter and persist them to a durable store (start with a Firestore-backed adapter in `server/src/collaboration/adapters/firestoreAdapter.ts`).

- **Source:** [Automation Initiative Roadmap](docs/automation-initiative-roadmap.md)
- **Section:** Not Done › Stability & Reliability (Protect the Experience)

## Task
Thread CRDT-aware operations through the adapter and persist them to a durable store (start with a Firestore-backed adapter in `server/src/collaboration/adapters/firestoreAdapter.ts`).

## Acceptance Criteria
- [ ] Thread CRDT-aware operations through the adapter and persist them to a durable store (start with a Firestore-backed adapter in `server/src/collaboration/adapters/firestoreAdapter.ts`).
