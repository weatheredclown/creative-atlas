# Automation Initiative Roadmap — Not Done › Stability & Reliability (Protect the Experience): Add optimistic UI hooks in `code/src/` so editors can apply CRDT patches locally before the server ACK.

- **Source:** [Automation Initiative Roadmap](docs/automation-initiative-roadmap.md)
- **Section:** Not Done › Stability & Reliability (Protect the Experience)

## Task
Add optimistic UI hooks in `code/src/` so editors can apply CRDT patches locally before the server ACK.

## Acceptance Criteria
- [ ] Add optimistic UI hooks in `code/src/` so editors can apply CRDT patches locally before the server ACK.
